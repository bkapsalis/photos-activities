import 'package:flutter/material.dart';

class AppColors {
  // Primary
  static const Color primary = Color(0xFF4CAF50);
  static const Color primaryDark = Color(0xFF388E3C);
  static const Color primaryLight = Color(0xFF81C784);

  // Surface / Background
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFF5F5F5);
  static const Color background = Color(0xFFFAFAFA);

  // Text
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // Location Pill
  static const Color pillBackground = Color(0xDD2C2C2C);
  static const Color pillText = Color(0xFFFFFFFF);
  static const Color pillDot = Color(0xFF4CAF50);

  // Chat Bubbles
  static const Color chatBubbleOther = Color(0xFFF0F0F0);
  static const Color chatBubbleMe = Color(0xFF4CAF50);
  static const Color chatBubbleMeText = Color(0xFFFFFFFF);

  // Reactions
  static const Color heartRed = Color(0xFFE53935);
  static const Color heartPink = Color(0xFFFF5252);

  // Avatar palette
  static const List<Color> avatarColors = [
    Color(0xFFE53935), // Red
    Color(0xFF4CAF50), // Green
    Color(0xFF1E88E5), // Blue
    Color(0xFFFF9800), // Orange
    Color(0xFF8E24AA), // Purple
    Color(0xFF00ACC1), // Teal
    Color(0xFFD81B60), // Pink
    Color(0xFF6D4C41), // Brown
  ];

  // Web sidebar
  static const Color sidebarBackground = Color(0xFFF8F9FA);
  static const Color divider = Color(0xFFE0E0E0);

  // Navigation
  static const Color navActive = Color(0xFF4CAF50);
  static const Color navInactive = Color(0xFF9E9E9E);
}
